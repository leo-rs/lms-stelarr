<UserControl x:Class="STELARR2.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:STELARR2.Views"
             mc:Ignorable="d" BorderThickness="1" Background="Black"
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5">
            <Label Foreground="Wheat" FontSize="40" FontWeight="Bold" Margin="0,0,0,30">STELLAR</Label>
            <Label Foreground="White" Margin="5" HorizontalAlignment="Center">Username</Label>
            <TextBox x:Name="Username"></TextBox>
            <Label Foreground="White" Margin="5" HorizontalAlignment="Center">Password</Label>
            <TextBox x:Name="Password"></TextBox>

            <Button x:Name="Login" Margin="5,30,5,5" Grid.Row="1" Width="100">Login</Button>
            <Button  Width="100">Create Account</Button>

            <TextBlock x:Name="WarningIsVisible" HorizontalAlignment="Center" Foreground="White" Background="Green" Visibility="Hidden">Login Successful</TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
